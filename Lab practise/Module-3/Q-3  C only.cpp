#include<stdio.h> /* Area of Rectangle in POP.*/
main()
{
	float a,b;
	printf("Enter the length(meter):");
	scanf("%f",&a);
	printf("Enter the Width(meter):");
	scanf("%f",&b);
	printf("\n Area of Rectangle is=%f Square Meter",a*b);
	
}
